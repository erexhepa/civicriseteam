# Monorepo: configure "Base directory" = "frontend" nas configurações do site na Netlify
[build]
  base = "."
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
